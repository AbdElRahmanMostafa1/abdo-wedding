import { Playfair_Display } from "next/font/google";

const playfairDisplay = Playfair_Display({
    weight: ["900"],
    subsets: ["latin"],
    display: "swap",
});

export {
    playfairDisplay
}