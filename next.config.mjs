/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'jd.exsythemes.com'
            }
        ]
    },
};

export default nextConfig;
